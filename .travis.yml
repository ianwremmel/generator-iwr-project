language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-iwr-project' ]; then cd .. && eval "mv $currentfolder generator-iwr-project" && cd generator-iwr-project; fi

