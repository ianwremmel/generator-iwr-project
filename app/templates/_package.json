{
  "name": "<%= _.slugify(appname) %>",
  "version": "0.0.0",
  "main": "src/index.js",
  "dependencies": {<% if (useBower) { %>
    "bower": "^1.3.9",<% } %><% if (useBrowserify) { %>
    "browserify": "^5.11.0",<% } %><% if (this.unitTests || this.integrationTests || this.acceptanceTests) { %>
    "chai": "^1.9.1",<% } %><% if (useBrowserify) { %>
    "debowerify": "^0.8.1",<% } %>
    "grunt": "^0.4.5",
    "grunt-bump": "0.0.15",
    "grunt-cli": "^0.1.13",
    "grunt-contrib-jshint": "^0.10.0",
    "grunt-jscs": "^0.7.0",
    "grunt-shell": "^1.0.1",<% if (useBrowserify) { %>
    "jshint-stylish": "^0.4.0",<% } %><% if (unitTests || integrationTests) { %>
    "karma": "^0.12.23",
    "karma-browserify": "^0.2.1",
    "karma-chrome-launcher": "^0.1.4",
    "karma-firefox-launcher": "^0.1.3",
    "karma-mocha": "^0.1.9",
    "karma-mocha-reporter": "^0.3.1",
    "karma-safari-launcher": "^0.1.1",
    "load-grunt-tasks": "^0.6.0",<% } %><% if (this.unitTests || this.integrationTests || this.acceptanceTests) { %>
    "mocha": "^1.21.4",
    "sinon": "^1.10.3",<% } %>
    "time-grunt": "^1.0.0"
  },
  "scripts": {
    "test": "./node_modules/.bin/grunt test"
  },
  "private": true
}
